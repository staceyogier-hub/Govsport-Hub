# Govsport Hub Contracts (Draft)

## API
- Base URL via `GOVSPORT_API_BASE_URL`
- Endpoints (placeholder)
  - `GET /devices`
  - `POST /sessions`

## Edge Protocol
- Versioned packet format managed in `packages/edge-protocol`
